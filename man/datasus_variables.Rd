% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary_inspection.R
\name{datasus_variables}
\alias{datasus_variables}
\title{Consult variables in an official DataSUS dictionary}
\usage{
datasus_variables(
  information_system,
  include_labels = TRUE,
  timeout = 240,
  refresh = FALSE,
  quiet = FALSE,
  cache_dir = getOption("microdatasus.cache_dir", NULL),
  fields = NULL,
  view = c("definitions", "fields"),
  include_ranges = TRUE
)
}
\arguments{
\item{information_system}{A value accepted by fetch_tabwin_dictionary().}

\item{include_labels}{Logical scalar. If TRUE, parse CNV and DBF relations
and include their code-label tables in the labels list-column.}

\item{timeout}{A positive numeric scalar. Download and connection timeout,
in seconds.}

\item{refresh}{Logical scalar. If \code{TRUE}, discard the session cache and
download the archive again.}

\item{quiet}{Logical scalar. If \code{TRUE}, suppress download progress and
status messages.}

\item{cache_dir}{Optional persistent cache root. The package option
\code{microdatasus.cache_dir} is used by default; \code{NULL} uses only the current
session cache.}

\item{fields}{Optional character vector restricting the returned fields.}

\item{view}{Either \code{"definitions"}, with one row per DEF declaration, or
\code{"fields"}, with repeated declarations grouped in a list-column.}

\item{include_ranges}{Logical scalar. If TRUE, include symbolic CNV interval
rules in the \code{ranges} list-column.}
}
\value{
A tibble with one row per categorical definition or numeric field.
}
\description{
Downloads or reuses a TabWin dictionary and presents its variable metadata
and code-label maps as a rectangular lookup table.
}
\details{
Large analytical CNV ranges are retained as symbolic rules instead
of being expanded into millions of rows. \code{status} distinguishes complete,
fallback, non-enumerable, missing, invalid, and failed relations. A
\code{issue_class} independently identifies upstream absence/content drift,
archive ambiguity, parser/I/O errors, analytical ranges, or definition
fallback, so severity is not confused with origin.
Fallback is reported when an official two-column DBF renamed its sole
description field while the DEF retained the previous name. DBFs with
duplicate keys and conflicting labels are also explicit fallbacks: the
last physical record is retained, following TabWin precedence. A CNV whose
declared category count differs from its physical definitions is reported
as an upstream fallback, while every physical category is retained. Parsed
relations persist on disk when \code{cache_dir} is set, and completed result
tables are reused during the R session.
}
\examples{
\dontshow{if (interactive() && curl::has_internet()) withAutoprint(\{ # examplesIf}
variables <- datasus_variables("SIM-DO", include_labels = FALSE)
variables[, c("field", "type", "description")]
\dontshow{\}) # examplesIf}
}
