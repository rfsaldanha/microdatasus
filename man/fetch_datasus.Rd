% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_datasus.R
\name{fetch_datasus}
\alias{fetch_datasus}
\title{Download DataSUS microdata}
\usage{
fetch_datasus(
  year_start,
  month_start = NULL,
  year_end,
  month_end = NULL,
  uf = "all",
  information_system,
  vars = NULL,
  stop_on_error = FALSE,
  timeout = 240,
  track_source = FALSE,
  quiet = FALSE,
  cache_dir = getOption("microdatasus.cache_dir", NULL),
  refresh = FALSE,
  destination = NULL,
  collect = TRUE,
  process = FALSE,
  process_args = list(),
  provenance = FALSE,
  keep_files = FALSE,
  row_filter = NULL
)
}
\arguments{
\item{year_start, year_end}{Numeric scalars giving the first and last requested
years, inclusive.}

\item{month_start, month_end}{Numeric scalars giving the first and last
requested months, inclusive. Months are required for SIH, SIA, and CNES
systems and ignored, with a warning, for annual systems.}

\item{uf}{A Brazilian state abbreviation, a character vector of
abbreviations, or \code{"all"}. \code{"all"} cannot be combined with individual
states. A warning alert is displayed when this argument is ignored for
systems published only as national files.}

\item{information_system}{A single system identifier listed in
\strong{Supported systems}.}

\item{vars}{\code{NULL}, or a character vector of column names to retain. Selection
is applied to each file before the files are combined.}

\item{stop_on_error}{Logical scalar. If \code{TRUE}, abort after any listing,
download, or read failure. If \code{FALSE}, warn and return the files that could
be read successfully.}

\item{timeout}{A positive numeric scalar giving the connection and transfer
timeout, in seconds, for each network attempt.}

\item{track_source}{Logical scalar. If \code{TRUE}, append a \code{source} column with
the original DBC file name. This column is retained even when \code{vars} is
supplied. The function aborts if the downloaded data already contain a
column named \code{source}.}

\item{quiet}{Logical scalar. If \code{FALSE} (the default), display the transfer
progress reported by \code{\link[curl:curl_download]{curl::curl_download()}} and announce each file before
downloading it. If \code{TRUE}, suppress status messages, per-file
announcements, and progress meters. Warnings and errors are not suppressed.}

\item{cache_dir}{Optional directory used as a persistent cache for downloaded
DBC files and TabWin dictionaries. The \code{microdatasus.cache_dir} option
supplies the default; \code{NULL} preserves the historical session-temporary
behavior.}

\item{refresh}{Logical scalar. If \code{TRUE}, download files again even when a
valid cached copy is available.}

\item{destination}{Optional directory in which each prepared file is saved
separately as RDS. This supports requests too large to combine in memory.}

\item{collect}{Logical scalar. If \code{TRUE} (the default), combine and return
records as before. If \code{FALSE}, write per-file RDS outputs and return their
provenance table; in that case \code{destination} is required.}

\item{process}{Logical scalar. If \code{TRUE}, run the processor matching
\code{information_system} independently on each downloaded file.}

\item{process_args}{Named list of additional arguments passed to the matching
\verb{process_*()} function. It cannot replace \code{data} or \code{information_system}.}

\item{provenance}{Logical scalar. If \code{TRUE}, attach download URLs, checksums,
sizes, timestamps, cache status, and paths. Retrieve the table with
\code{\link[=datasus_provenance]{datasus_provenance()}}.}

\item{keep_files}{Logical scalar. If \code{TRUE} and \code{destination} is supplied,
retain a copy of each raw DBC file under \code{destination/dbc}.}

\item{row_filter}{Optional function called on each raw DBC table immediately
after reading and before processing or column selection. It must return one
non-missing logical value per row. This bounds downstream processing and
output without changing which source files are downloaded.}
}
\value{
With \code{collect = TRUE}, a tibble containing all successfully read
records, or \code{NULL} if no requested file could be read. With
\code{collect = FALSE}, a provenance tibble with one row per output file.
}
\description{
Downloads published DBC files from DataSUS, reads them with \code{\link[=read_dbc]{read_dbc()}},
and combines the records in deterministic period, state, and file-part order.
}
\details{
The function first lists the relevant DataSUS directories and downloads only
files present in those listings. When more than one publication represents
the same system, period, state, and file part, definitive/current data take
precedence over preliminary data, and current data take precedence over
historical copies.

Files are handled sequentially and, when requested, processed and written
before the next file is read. Thus \code{collect = FALSE} bounds working memory
to approximately one source file. Unless \code{quiet = TRUE}, transfer progress is
displayed by \code{\link[curl:curl_download]{curl::curl_download()}}. Transient network failures are retried
up to two times; missing, empty, invalid DBC, and incompatible-schema files
are not retried. Partial files and other temporary files are removed before
the function returns or aborts.

When \code{cache_dir} is supplied, complete DBC files and dictionaries persist
across R sessions. Cache entries include a manifest and SHA-256 checksum (while still accepting legacy MD5 manifests).
\code{\link[=datasus_cache_info]{datasus_cache_info()}} inspects them and \code{\link[=clear_datasus_cache]{clear_datasus_cache()}} removes only
files managed by microdatasus.

Years and state abbreviations refer to DataSUS processing periods and places
of processing, which may differ from dates or places of occurrence and
residence contained in the records.
}
\section{Supported systems}{

\itemize{
\item \strong{SIH:} \code{"SIH-RD"}, \code{"SIH-RJ"}, \code{"SIH-SP"}, and \code{"SIH-ER"}.
\item \strong{SIM:} \code{"SIM-DO"}, \code{"SIM-DOFET"}, \code{"SIM-DOEXT"}, \code{"SIM-DOINF"}, and
\code{"SIM-DOMAT"}.
\item \strong{SINASC:} \code{"SINASC"}.
\item \strong{CNES:} \code{"CNES-LT"}, \code{"CNES-ST"}, \code{"CNES-DC"}, \code{"CNES-EQ"},
\code{"CNES-SR"}, \code{"CNES-HB"}, \code{"CNES-PF"}, \code{"CNES-EP"}, \code{"CNES-RC"},
\code{"CNES-IN"}, \code{"CNES-EE"}, \code{"CNES-EF"}, and \code{"CNES-GM"}.
\item \strong{SIA:} \code{"SIA-AB"}, \code{"SIA-ABO"}, \code{"SIA-ACF"}, \code{"SIA-AD"}, \code{"SIA-AN"},
\code{"SIA-AM"}, \code{"SIA-AQ"}, \code{"SIA-AR"}, \code{"SIA-ATD"}, \code{"SIA-PA"}, \code{"SIA-PS"},
and \code{"SIA-SAD"}.
\item \strong{SINAN:} 58 readable identifiers, including \code{"SINAN-DENGUE"},
\code{"SINAN-TUBERCULOSE"}, and
\code{"SINAN-ACIDENTE-POR-ANIMAIS-PECONHENTOS"}. Former acronym-based
identifiers remain accepted as aliases. The complete lookup table is
returned by \code{\link[=datasus_information_systems]{datasus_information_systems()}}.
}
}

\section{Network access}{

An Internet connection and FTP access to DataSUS are required. DataSUS may
restrict FTP access from some countries. Interrupted transfers are resumed
when supported. Alternative base URLs can be configured with the
\code{microdatasus.mirrors} option; the official DataSUS URL is always tried
first.
}

\examples{
\dontshow{if (interactive() && curl::has_internet()) withAutoprint(\{ # examplesIf}
sim <- fetch_datasus(
  year_start = 2014,
  year_end = 2014,
  uf = "AC",
  information_system = "SIM-DO",
  vars = c("CODMUNRES", "DTOBITO", "CAUSABAS")
)

sih <- fetch_datasus(
  year_start = 2014,
  month_start = 1,
  year_end = 2014,
  month_end = 2,
  uf = c("AC", "RR"),
  information_system = "SIH-RD"
)
\dontshow{\}) # examplesIf}
}
\references{
Saldanha, R. F. (2026). \href{https://rfsaldanha.github.io/sis/}{\emph{Sistemas de Informação em Saúde no Brasil}}, especially the chapters on
\href{https://rfsaldanha.github.io/sis/sim.html}{SIM},
\href{https://rfsaldanha.github.io/sis/sinasc.html}{SINASC},
\href{https://rfsaldanha.github.io/sis/sih.html}{SIH},
\href{https://rfsaldanha.github.io/sis/sia.html}{SIA},
\href{https://rfsaldanha.github.io/sis/sinan.html}{SINAN}, and
\href{https://rfsaldanha.github.io/sis/cnes.html}{CNES}.
}
\seealso{
\code{\link[=read_dbc]{read_dbc()}} for local DBC files; \code{\link[=datasus_information_systems]{datasus_information_systems()}} for the
complete identifier lookup; \code{\link[=process_sim]{process_sim()}}, \code{\link[=process_sinasc]{process_sinasc()}},
\code{\link[=process_sih]{process_sih()}}, \code{\link[=process_sia]{process_sia()}}, \code{\link[=process_cnes]{process_cnes()}}, and \code{\link[=process_sinan]{process_sinan()}}
for system-specific recoding.
}
