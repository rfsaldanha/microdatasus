% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema_validation.R
\name{validate_datasus_schema}
\alias{validate_datasus_schema}
\title{Validate a DBC table against its dictionary and processor}
\usage{
validate_datasus_schema(
  data,
  information_system,
  process = TRUE,
  period = NULL,
  timeout = 240,
  refresh = FALSE,
  quiet = FALSE,
  cache_dir = getOption("microdatasus.cache_dir", NULL),
  sample_n = 100L,
  sample_by_dictionary = TRUE
)
}
\arguments{
\item{data}{A data frame returned by \code{\link[=fetch_datasus]{fetch_datasus()}} or one local DBC
path. Reading a path loads that DBC in memory.}

\item{information_system}{One public value listed by
\code{\link[=datasus_information_systems]{datasus_information_systems()}}.}

\item{process}{Logical scalar. If \code{TRUE}, process one representative row
to report the resulting column types without processing the full table.}

\item{period}{Optional scalar label stored in the returned contract, such
as a year or competence. Historical DEF selection uses fields in \code{data}.}

\item{timeout}{A positive numeric scalar. Download and connection timeout,
in seconds.}

\item{refresh}{Logical scalar. If \code{TRUE}, discard the session cache and
download the archive again.}

\item{quiet}{Logical scalar. If \code{TRUE}, suppress download progress and
status messages.}

\item{cache_dir}{Optional persistent cache root. The package option
\code{microdatasus.cache_dir} is used by default; \code{NULL} uses only the current
session cache.}

\item{sample_n}{Positive integer giving the maximum deterministic sample
size processed for each represented historical dictionary.}

\item{sample_by_dictionary}{Logical scalar. If \code{TRUE}, sample every
represented current or historical definition instead of only the table
as a whole.}
}
\value{
A tibble with one row per field observed, declared by a selected
DEF, or added by the processor. \code{status} distinguishes matched,
observed-only, dictionary-only, and processor-added fields.
}
\description{
Builds a field-level contract from a raw DataSUS table (or local DBC),
the official DEF selected for each represented period, and the column
classes produced by the corresponding \verb{process_*()} function.
}
