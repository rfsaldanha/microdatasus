% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_sinan.R
\name{process_sinan}
\alias{process_sinan}
\title{Prepare SINAN notification microdata}
\usage{
process_sinan(
  data,
  information_system = "SINAN-DENGUE",
  municipality_data = TRUE,
  labels = c("factor", "character", "none"),
  diagnostics = FALSE
)
}
\arguments{
\item{data}{A data frame returned by \code{\link[=fetch_datasus]{fetch_datasus()}} for a supported SINAN
file family, or another data frame with a compatible layout.}

\item{information_system}{SINAN file family represented by \code{data}. Preferred
values use readable names such as \code{"SINAN-DENGUE"} and
\code{"SINAN-TUBERCULOSE"}. All former acronym-based values remain accepted as
aliases. Use \code{\link[=datasus_information_systems]{datasus_information_systems()}} and filter \code{system == "SINAN"}
to consult both forms.}

\item{municipality_data}{Logical scalar. If \code{TRUE}, add municipality names
and available territorial attributes. The historical \code{MUNICIPIO} field is
preferred when present, followed by residence and notification fields.}

\item{labels}{Output type for categorical labels: \code{"factor"} (the default),
\code{"character"}, or \code{"none"} to retain the original codes.}

\item{diagnostics}{Logical scalar. If \code{TRUE}, attach a processing report,
including codes absent from official conversion tables. Retrieve it with
\code{\link[=processing_diagnostics]{processing_diagnostics()}}.}
}
\value{
A tibble. Dates are returned as \code{Date}, the notification year and
derived age components as integer, labelled categorical fields as factors,
and identifiers and free text as character.
}
\description{
Uses the official DataSUS TabWin definitions to label all SINAN file
families supported by \code{\link[=fetch_datasus]{fetch_datasus()}}. The corresponding
\code{TAB_SINANNET.zip} or \code{TAB_SINANONLINE.zip} archive is downloaded on first
use and cached for the rest of the R session. When DataSUS publishes no
disease-specific DEF, the official \code{NotIndiviNet.def} supplies labels for
common notification fields; unmapped disease-specific codes remain visible.
}
\examples{
\dontshow{if (interactive() && curl::has_internet()) withAutoprint(\{ # examplesIf}
process_sinan(sinan_dengue_sample, "SINAN-DENGUE")
process_sinan(sinan_chagas_sample, "SINAN-DOENCA-DE-CHAGAS-AGUDA")
\dontshow{\}) # examplesIf}
}
\references{
Saldanha, R. F. (2026). \href{https://rfsaldanha.github.io/sis/sinan.html}{SINAN -- Sistema de Informação de Agravos de Notificação}.
}
\seealso{
\code{\link[=datasus_information_systems]{datasus_information_systems()}}, \code{\link[=fetch_tabwin_dictionary]{fetch_tabwin_dictionary()}},
\code{\link[=fetch_datasus]{fetch_datasus()}}
}
