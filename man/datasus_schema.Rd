% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary_audit.R
\name{datasus_schema}
\alias{datasus_schema}
\title{Build a dictionary-derived DataSUS schema contract}
\usage{
datasus_schema(
  information_system,
  inspect = FALSE,
  timeout = 240,
  refresh = FALSE,
  quiet = FALSE,
  cache_dir = getOption("microdatasus.cache_dir", NULL)
)
}
\arguments{
\item{information_system}{One dictionary key accepted by
\code{\link[=fetch_tabwin_dictionary]{fetch_tabwin_dictionary()}}.}

\item{inspect}{Logical scalar. If \code{TRUE}, parse relations and include their
status; otherwise build the contract from DEF metadata only.}

\item{timeout}{A positive numeric scalar. Download and connection timeout,
in seconds.}

\item{refresh}{Logical scalar. If \code{TRUE}, discard the session cache and
download the archive again.}

\item{quiet}{Logical scalar. If \code{TRUE}, suppress download progress and
status messages.}

\item{cache_dir}{Optional persistent cache root. The package option
\code{microdatasus.cache_dir} is used by default; \code{NULL} uses only the current
session cache.}
}
\value{
A tibble with one row per field and list-columns containing every
description, relation, file, command, and status declared for that field.
}
\description{
Summarises all fields declared by one official TabWin DEF. The contract
describes dictionary roles and relations; fields absent from the DEF, such as
free text, remain discoverable only in the corresponding DBC layout.
}
