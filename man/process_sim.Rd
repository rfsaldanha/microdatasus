% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_sim.R
\name{process_sim}
\alias{process_sim}
\title{Prepare SIM mortality microdata}
\usage{
process_sim(
  data,
  municipality_data = TRUE,
  information_system = "SIM-DO",
  labels = c("factor", "character", "none"),
  diagnostics = FALSE
)
}
\arguments{
\item{data}{A data frame returned by \code{\link[=fetch_datasus]{fetch_datasus()}} for a supported SIM
mortality type, or another data frame with a compatible layout.}

\item{municipality_data}{Logical scalar. If \code{TRUE}, add municipality names
and available territorial attributes for \code{CODMUNRES}.}

\item{information_system}{SIM data type represented by \code{data}. One of
\code{"SIM-DO"}, \code{"SIM-DOFET"}, \code{"SIM-DOEXT"}, \code{"SIM-DOINF"}, or
\code{"SIM-DOMAT"}. The default preserves the previous \code{process_sim()} call.}

\item{labels}{Output type for categorical labels: \code{"factor"} (the default),
\code{"character"}, or \code{"none"} to retain the original codes.}

\item{diagnostics}{Logical scalar. If \code{TRUE}, attach a processing report,
including codes absent from official conversion tables. Retrieve it with
\code{\link[=processing_diagnostics]{processing_diagnostics()}}.}
}
\value{
A tibble. Dates are returned as \code{Date}, quantities as integer,
labelled categorical fields as factors, and identifiers and free text as
character.
}
\description{
Uses the official DataSUS TabWin CID-10 and historical CID-9 dictionaries
to label supported SIM mortality fields with period-correct domains. The dictionary is
downloaded on first use and cached for the rest of the R session. Dates,
integer quantities, categorical variables, and identifier fields retain
distinct and consistent types.
}
\details{
Codes not covered by the applicable TabWin conversion are retained as
factor levels instead of being silently discarded. Historical fields kept
in early CID-10 fetal-death files use their original CID-9 definitions.
Official numeric missing sentinels and out-of-domain measurements are
returned as \code{NA}; malformed values are included in diagnostics.
}
\section{Performance and cache}{

Processing uses vectorized code padding and CNV thresholds, parses repeated
dates once per field and format, and unescapes only text containing
backslashes. UTF-8 conversion is still performed for text; values marked as
\code{"bytes"} bypass text normalization. Historical relation selection subsets
only the source columns it needs. These optimizations are automatic.

Dictionaries are reused within the R session. For reuse across sessions,
set \code{options(microdatasus.cache_dir = datasus_cache_dir(create = TRUE))};
calling \code{\link[=datasus_cache_dir]{datasus_cache_dir()}} alone does not enable persistent caching.
The first processing call can include dictionary downloads and parsing.
\code{labels = "none"} controls categorical output, not network access: some
processors still need DEF metadata or relations for field semantics.

\code{diagnostics = FALSE} avoids collecting the optional report, and
\code{municipality_data = FALSE} omits territorial enrichment when it is not
needed. For requests spanning many files, use \code{\link[=fetch_datasus]{fetch_datasus()}} with
\code{process = TRUE}, \code{collect = FALSE}, and \code{destination} to save each file
separately. A processor called directly still holds its input and output
in memory. See the \href{https://rfsaldanha.github.io/microdatasus/articles/dicionarios-cache-e-escala.html}{processing guide}.

Territorial enrichment uses the fixed \link{tabMun} snapshot identified by
\code{\link[=datasus_reference_tables]{datasus_reference_tables()}}, not an automatically selected edition for each
observation year. Enable diagnostics to record that version in the report.
}

\examples{
\dontshow{if (interactive() && curl::has_internet()) withAutoprint(\{ # examplesIf}
process_sim(sim_do_sample)
\dontshow{\}) # examplesIf}
}
\references{
Saldanha, R. F. (2026). \href{https://rfsaldanha.github.io/sis/sim.html}{SIM -- Sistema de Informação sobre Mortalidade}.
}
\seealso{
\code{\link[=fetch_tabwin_dictionary]{fetch_tabwin_dictionary()}}, \code{\link[=fetch_datasus]{fetch_datasus()}}
}
