# Fetch CADGER table

This function fetchs an updated CADGER table from DataSUS

## Usage

``` r
fetch_cadger(timeout = 240)
```

## Arguments

- timeout:

  sets the download timeout reference. Defaults to 240

## Value

a data.frame
